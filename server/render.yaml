services:
  - type: web
    name: afb-wallet-server
    env: node
    plan: free
    buildCommand: npm install && npx prisma generate && npx prisma db push && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        value: file:./dev.db
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: GMAIL_USER
        value: slafpay4@gmail.com
      - key: GMAIL_APP_PASSWORD
        value: qfhh lfaw nfnt erfc
